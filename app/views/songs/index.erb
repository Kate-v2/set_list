

<% @songs.each do |song| %>
  <ul>
    <li><%= song.title %></li>
    <li><%= song.length %></li>
    <li><%= song.play_count %></li>
    <li><%= song.playlist.name if song.playlist %></li>
  </ul>
<%end%>
